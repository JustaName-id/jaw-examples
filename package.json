{
  "name": "jaw-examples",
  "private": true,
  "workspaces": ["examples/*"],
  "scripts": {
    "dev": "nx dev",
    "build": "nx run-many -t build"
  },
  "devDependencies": {
    "nx": "latest",
    "@nx/next": "latest",
    "@nx/vite": "latest"
  }
}
