{
  "name": "@jaw-examples/eip7702-node-quickstart",
  "version": "0.1.0",
  "private": true,
  "nx": {
    "name": "eip7702-node-quickstart",
    "projectType": "application",
    "targets": {
      "dev": {
        "executor": "nx:run-commands",
        "options": {
          "command": "tsx src/index.ts",
          "cwd": "{projectRoot}"
        }
      },
      "build": {
        "executor": "nx:run-commands",
        "outputs": ["{projectRoot}/dist"],
        "options": {
          "command": "tsc",
          "cwd": "{projectRoot}"
        }
      }
    }
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc"
  },
  "dependencies": {
    "@jaw.id/core": "latest",
    "dotenv": "^16",
    "viem": "^2"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4",
    "typescript": "^5"
  }
}
